# Daily Audit — Friday 2026-07-24 (first intraday trading day)

*Written manually by the main Cowork session — the scheduled 4:30 audit fired but couldn't reach the Mac (see Architecture note).*

## Summary

First day of 30-minute intraday mode. **13 trading cycles ran on schedule** (09:47–15:14 ET, ~every 30 min), plus correctly-skipped out-of-window firings before open and after 15:15. **Zero trades** — every candidate skipped on confidence (range 40–62% vs 65% floor, "signals disagreed"), which is honest behavior on a mixed Friday, not a malfunction. No entries after the 15:30 cutoff. Equity $4,023–$4,032 through the day (~flat).

## Incidents

1. **Morning crash (fixed same day):** ZeroDivisionError in position_monitor — the BTCUSD position has avg_entry_price=0 from Alpaca. Cycles errored 09:47–10:03 until a skip-guard was deployed. That BTC position (~$1.4k, 35% of account) remains UNMANAGED (no stop/exits possible without an entry price) — Shawn to decide: sell manually or hold unprotected.
2. **SEER orphan:** the 09:56 launcher start left an orphaned scanner with a broken log pipe (terminal SIGHUP killed its parent). Orphan killed; launcher now starts scanner.py directly under nohup.

## Bugs found by this audit (fixed tonight)

3. **BTCUSD invalid-symbol error every cycle** — `tickers` contained `BTCUSD`, but Alpaca crypto requires `BTC/USD` (which is already in crypto.tickers). Removed from config.yaml and tickers_auto.json. The 09:00 rotation had even flagged it as a duplicate.
4. **VIX gate was still dead** — `yfinance` wasn't installed in the .venv, so the new VIX fetch failed silently ("No module named yfinance") and VIX stayed 0. Installed yfinance 1.5.2; the gate becomes live next cycle.

## Also verified

- 09:00 pre-market watchlist rotation ran: churn 0, kept the current list, rails respected.
- Confidence scoring shows the rebalanced weights in output (agreement % + direction logged per ticker).
- Dexter gate initialized; no vetoes needed (no entries attempted).

## Architecture note → action taken

Scheduled Cowork audits run in fresh cloud sessions with NO connection to the Mac (folder grants don't carry over), so they couldn't read logs or write this report. Fix applied: the post-close audit trigger now falls back to querying the Alpaca paper API directly (orders/positions/equity) when the Mac is unreachable, so trade-level auditing works unattended; log-level detail (cycle timing, gate activity) still requires the desktop app open. Long-term option on the roadmap: sync bot status/logs to Supabase so audits are fully independent.

## Open items

- Sell or accept the unmanaged BTCUSD position (Shawn's call)
- SEER live-vs-paper decision (still open; SEER live-scanned today, spread gates correctly rejected thin arbs)
- Rotate all API keys (both repos)
